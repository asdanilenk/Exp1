<Window x:Class="Exp1.EditRuleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EditRule" Height="300" Width="545" WindowStartupLocation="CenterOwner" MinWidth="545" MinHeight="300">
    <Grid>
        <ScrollViewer VerticalAlignment="Stretch" Margin="10,10,10,40" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid Name="editRule">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <WrapPanel Grid.Row="1" >
                    <TextBlock Text="Conditions:" VerticalAlignment="Center" />
                    <Button Width="20" Height="20" Margin="10,0,0,0" Click="Button_Click">
                        <Image Source="Resources\add.png" Height="16" />
                    </Button>
                </WrapPanel>
            </Grid>
        </ScrollViewer>
        <Button Content="Cancel" Height="23" HorizontalAlignment="Right" Margin="0,0,10,10" Name="cancelButton" VerticalAlignment="Bottom" Width="75" Click="cancelButton_Click" />
        <Button Content="Save" Height="23" HorizontalAlignment="Right" Margin="0,0,100,10" Name="saveButton" VerticalAlignment="Bottom" Width="75" Click="saveButton_Click" />
    </Grid>
</Window>
