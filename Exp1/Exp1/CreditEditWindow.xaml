<Window x:Class="Exp1.CreditEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="600" Width="800" WindowStartupLocation="CenterOwner">
    <Grid>
        <ScrollViewer Margin="5,30,5,35" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid Name="Params">

            </Grid>
        </ScrollViewer>
        <TextBlock Height="23" HorizontalAlignment="Left" Margin="8,7,0,0" Text="Параметры:" VerticalAlignment="Top" />
        <Button Content="Отменить" Height="23" HorizontalAlignment="Right" Margin="0,0,10,10" Name="cancelButton" VerticalAlignment="Bottom" Width="75" Click="cancelButton_Click" />
        <Button Content="Сохранить" Height="23" HorizontalAlignment="Right" Margin="0,0,100,10" Name="saveButton" VerticalAlignment="Bottom" Width="75" Click="saveButton_Click" />
    </Grid>
</Window>
